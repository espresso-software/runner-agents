---
- name: Deploy DEV Agent
  hosts:
    - "{{ agent_cluster | default('test-k3s-managers') }}"
  run_once: true
  become: true
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  roles:
    - cleanup
